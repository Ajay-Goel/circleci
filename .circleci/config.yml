  version: 2.1
  jobs:
    build:
      docker: 
        - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
      steps:
        - checkout # check out the code in the project directory
        - run: echo "hello world Ajay" # run the `echo` command
        - run: mkdir codedeploy-artifact
        - run: ls
        - run: zip -r csye6225-webapp-${CIRCLE_BUILD_NUM}.zip webapp
        - run: pwd
        - run: ls -al
        - run: mv csye6225-webapp-${CIRCLE_BUILD_NUM}.zip codedeploy-artifact/
        - run: ls -al
        - run: pwd
        - run: cd codedeploy-artifact
        - run: pwd
        - run: ls -al
        - run: cd ..
        - run: pwd
        - run: ls -al